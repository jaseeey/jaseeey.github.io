image: node:latest

pages:
  stage: deploy
  before_script:
    - npm install
  script:
    - npm run build
    - test -f build/index.html
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
